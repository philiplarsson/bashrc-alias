# ----------------- ALIAS -------------------

# This alias do colored output on ls
alias ls="ls -G"

# Simple shortcut to clear
alias c="clear"

# Asks before overwrite existing file
alias mv="mv -i"

# This alias reloads this file. Change to .bashrc on linux.
alias reload_profile=". ~/.profile"

# To move upp one folder
alias ..="cd ../"

#Compiles C using C99 Standard and extra warnings
alias cc='cc -std=c99 -pedantic -Wextra'